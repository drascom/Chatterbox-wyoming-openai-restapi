# requirements.txt
# Tested with Python 3.11; other versions may hit protobuf pinning issues with some deps.

chatterbox-tts

# Core Web Framework
fastapi
uvicorn[standard]

# Machine Learning & Audio
torch
torchaudio
numpy
soundfile # Requires libsndfile system library (e.g., sudo apt-get install libsndfile1 on Debian/Ubuntu)
huggingface_hub
safetensors
wyoming
openai-whisper
onnx==1.15.0  # matches protobuf>=3.20.3,<4 and has wheels for py3.11
protobuf>=3.20.3,<4.0.0  # aligns onnx with the rest of the stack
resemble-perth==1.0.1  # required by chatterbox-tts watermarker
setuptools  # needed for pkg_resources used by resemble-perth

# Configuration & Utilities
pydantic
python-dotenv # Used ONLY for initial config seeding if config.yaml missing
Jinja2
python-multipart # For file uploads
requests # For health checks or other potential uses
PyYAML # For parsing presets.yaml AND primary config.yaml
tqdm

# Audio Post-processing
pydub
praat-parselmouth # For unvoiced segment removal
librosa # for changes to sampling
hf-transfer
peft # Needed by diffusers to use PEFT backend and silence LoRACompatibleLinear deprecation
